# Dokimisu

Minimal single-page website for Dokimisu, a weekend tiramisu clinic by one actual doctor.

## Development

```bash
npm install
npm run dev
npm run build
```

## WhatsApp number

Update `WHATSAPP_NUMBER` in `src/main.js` with the real international phone number, without `+` or spaces.
